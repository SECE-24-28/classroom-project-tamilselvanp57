import React from 'react'

const Header = (props) => {
  return (
    // <div>
    //     <h2>{props.title}-{props.dep}</h2>
    // </div>
    <div>
        <h1>{props.title}</h1>
    </div>
  )
}

export default Header
